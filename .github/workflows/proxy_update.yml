name: Party-Proxy Scrape & Check

on:
  schedule:
    - cron: '0 */12 * * *'  # Every 12 horas
  workflow_dispatch:  # Allow manual trigger

jobs:
  scrape_and_check:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Run Party-Proxy
        run: python party_proxy.py
      
      - name: Commit and push live proxies
        run: |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          git add output/active_proxies.txt README.md
          git diff --staged --quiet || git commit -m "🎉 Update proxies - $(date +'%Y-%m-%d %H:%M:%S UTC')" || echo "No changes to commit"
          git push || echo "Nothing to push"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
